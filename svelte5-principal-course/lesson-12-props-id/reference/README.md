# Lesson 12 reference — edge cases

| File | What it shows | Why it matters |
|---|---|---|
| `FormLabelling.svelte` | `$props.id()` for `<label for>` / `<input id>` pairs | Same rune, different ARIA pattern. Every form component uses this. |
