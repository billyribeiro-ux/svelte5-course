{
  "name": "svelte5-principal-course",
  "version": "0.0.0",
  "private": true,
  "description": "Billy Ribeiro's Principal Engineer Svelte 5 course",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "check": "pnpm -r --parallel run check",
    "build": "pnpm -r run build",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "prettier-plugin-svelte": "^3.5.1"
  }
}
