# Lesson 15 reference

No standalone examples — the README's equivalence table IS the reference. Open the two components side-by-side in `solution/src/lib/` to compare:

- `NoticeLegacy.svelte` — Svelte 4 slots + `<svelte:options runes={false}>`.
- `NoticeSnippets.svelte` — Svelte 5 snippets with typed `SlotArgs`.
